@{
    ViewData["Title"] = "Our Stylists";
}
@using HairSalon.Models;

<h1>Welcome to Our Stylists</h1>

@if (@Model.Count == 0)
{
    <h3>No stylists</h3>
}

<ol>
    @foreach (Stylist stylist in Model)
    {
        <li>@stylist.Name</li>
    }
</ol>